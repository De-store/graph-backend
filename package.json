{
  "name": "backend",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ destore/backend",
    "create-local": "graph create --node http://localhost:8020/ destore/backend",
    "remove-local": "graph remove --node http://localhost:8020/ destore/backend",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 destore/backend"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
